<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>Littles</title>
    <link rel="stylesheet" type="text/css" href="/css/main.css">
</head>

<body>
    <div class="navbar">
        {{# if currentUserId}}
            <a href="/littles/new" id="plus-icon-link"><img id="plus-icon" src="/img/plus.png" /></a>
            <div id="profile-icon-dropdown">
                <img id="profile-icon" src="/img/profile.png" />
            </div>

            <a class="nav-link" href="/logout">log out</a>
        {{else}}
            <a class="nav-link" href="/login">log in</a>
        {{/if}}
    </div>

    {{{body}}}

</body>

</html>
