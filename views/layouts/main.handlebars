<!doctype html>
<html>

<head>
    <meta charset="utf-8">
    <title>Littles</title>
</head>

<body>
    <a href="/littles/new">New Little</a>
    <div class="navbar-text">
        {{# if currentUserId}}
            <a class="nav-link" href="/logout">log out</a>
        {{else}}
            <a class="nav-link" href="/login">log in</a>
        {{/if}}
    </div>

    {{{body}}}

</body>

</html>
