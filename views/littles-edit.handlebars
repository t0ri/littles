<div class="row">
  <div class="col-sm-12 col-sm-offset-6">
    <form method="POST" action="/littles/{{littles._id}}?_method=PUT">
      {{> littles-form}}
      <!-- BUTTON -->
      <div class="form-group">
        <button type="submit">Save Little</button>
      </div>
      </form>
      <form method="POST" action="/littles/{{littles._id}}?_method=DELETE">
          <button type="submit">Delete</button>
      </form>
  </div>
</div>
